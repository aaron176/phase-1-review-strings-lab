// Write your code in this file!
const currentUser ="haroon"
const welcomeMessage = `Welcome to Recoded, + ${currentUser}`
const exitedWelcomeMessage = welcomeMessage.toUpperCase();
const shortGreeting =`Welcome ${exitedWelcomeMessage.slice(0,1)}`